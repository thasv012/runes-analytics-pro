const http = require('http'); const porta = 3003; console.log('Tentando iniciar servidor na porta '+porta); const servidor = http.createServer((req, res) => { res.writeHead(200, { 'Content-Type': 'text/plain' }); res.end('Teste OK!'); }); servidor.on('error', (err) => { console.error('ERRO: Porta '+porta+' já em uso!'); }); servidor.listen(porta, () => { console.log('SUCESSO! Servidor rodando na porta '+porta); });
